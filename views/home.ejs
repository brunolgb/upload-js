<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <title>Upload de arquivos</title>
    <style>
        *{
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }
        body{
            background: rgb(161, 225, 255);
        }
        #app{
            width: 500px;
            background: #fff;
            padding: 20px;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            border-radius: 2px;
            box-shadow: 5px 5px 5px rgba(0, 81, 87, 0.09);
        }
        #app *{
            width: 100%;
            text-align:center;
            font-size: 20px;
        }
        #app strong{
            display: block;
            margin-bottom: 30px;
            font-size: 2em;
            color: #888;
        }
        form label{
            display: flex;
            background: #fff;
            color: rgb(0, 121, 168);
            border: 1px solid rgb(0, 121, 168);
            outline: none;
            height: 60px;
            align-items: center;
            justify-content: center;
            border-radius: 2px;
            margin-bottom: 30px;
            cursor: pointer;
        }
        form input{
            display:none;
        }
        form button {
            display: flex;
            background: rgb(168, 0, 0);
            color: #fff;
            border: none;
            outline: none;
            height: 60px;
            align-items: center;
            justify-content: center;
            border-radius: 2px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <div id="app">
        <strong>Uploads de arquivos</strong>
        <form method="post" enctype="multipart/form-data" envioform>
            <label for="choice">Escolher um arquivo</label>
            <input type="file" name="img" envio id="choice">
            <button type='submit'>Enviar</button>
        </form>
    </div>
</body>
</html>